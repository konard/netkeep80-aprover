# Введение в Метатеорию Связей

> **Статус:** Готово ✅
> **Этап:** 5.2

## Что такое МТС?

**МТС есть язык паттернов форм связей.** Это не метафора и не аналогия — это онтологический фундамент системы.

В традиционных теориях символы ссылаются на внешние объекты: «стол» указывает на физический стол, «2» — на количество, «→» — на импликацию в некоторой модели. **МТС отвергает эту парадигму полностью.** Здесь нет «внешнего мира», на который ссылаются знаки. Есть только *паттерны форм связей*, которые сами являются связями.

### Отличие от классических систем

В отличие от классических логических систем (ZFC, теория типов, категории), МТС:

- **Не допускает онтологического дуализма** — нет разделения на "носитель" и "отношение"
- **Знак не ссылается на внешний объект** — знак является *запросом по форме* в "океане связей"
- **Доказательство = резолюция** — запрос по форме разрешается в асети связей

---

## Знак как паттерн формы

Когда мы пишем аксиому `∞ : (∞ -> ∞)`, символ `∞` не обозначает «бесконечность» как внешний объект. Он определяет *паттерн формы* — запрос ко всем связям, структура которых удовлетворяет условию «x = x -> x». Это не ссылка, а *формальный критерий отбора* из «океана связей».

**Знак в МТС — это всегда паттерн формы.** Он не указывает «туда», а задаёт правило различения форм здесь и сейчас.

### Примеры паттернов

| Символ | Паттерн формы | Запрос |
|--------|---------------|--------|
| `∞` | `∞ -> ∞` | Все связи формы `x = x -> x` |
| `♂x` | `♂x -> x` | Связи с самозамкнутым началом |
| `x♀` | `x -> x♀` | Связи с самозамкнутым концом |

---

## Рекурсивная система паттернов

Система аксиом МТС представляет собой **взаимно рекурсивную систему паттернов форм**. Каждая аксиома определяет паттерн, используя другие паттерны, и в совокупности они задают целостную онтологию.

```
∞   определяется через  ∞ -> ∞
♂x  определяется через  ♂x -> x
x♀  определяется через  x -> x♀
->  определяется через  ♂∞ -> ∞♀
```

Это не порочный круг, а **синхронное определение системы**. Все паттерны возникают одновременно в едином акте различения. Нет «первичных» и «вторичных» понятий — есть только взаимозависимая сеть форм.

---

## Как читать аксиомы

Формулы в системе аксиом МТС не являются уравнениями в классическом смысле. Они задают **структурные тождества паттернов форм**.

Когда мы пишем `♂x = ♂x -> x`, это означает: «Паттерн формы, обозначаемый как ♂x, структурно тождественен паттерну формы (♂x -> x)». Это не вычисление и не вывод — это констатация тождества форм.

### Ключевые символы

| Символ | Назначение |
|--------|------------|
| `:` | Определение паттерна («символ соответствует форме») |
| `=` | Структурная тождественность паттернов |
| `->` | Конструктор связи из двух элементов |
| `♂`, `♀` | Паттерны самозамыкания по началу/концу |
| `!` | Инверсия направленности паттерна |
| `{ }` | Шаблоны множественных связей |

---

## Два домена: формы и данные

Важно строго разделять два домена:

1. **Домен форм связей** — где задаются паттерны, аксиомы и структурные тождества
2. **Домен сериализации** — где связи представляются как последовательности символов

Формулы аксиом принадлежат первому домену. Они описывают паттерны форм, а не данные для обработки.

---

## Онтологическая чистота

МТС принципиально избегает:

- ❌ Внешних объектов («числа», «множества», «функции»)
- ❌ Скрытых параметров («контекст», «интерпретация», «модель»)
- ❌ Неявных предпосылок («допустим, что существует...»)

✅ Всё, что существует в МТС, явно задаётся паттернами форм связей. Если паттерн не определён аксиомами — его нет в системе.

---

## С чего начинается МТС?

МТС начинается не с объектов, не с аксиом, не с логики. **МТС начинается с различения.**

```
Первое различие:  между тем, что было, и тем, что стало
Второе различие: между первым различием и новым состоянием
Третье различие: между вторым различием и следующим состоянием
...
```

Эта последовательность различений и есть первичная реальность для субъекта. МТС даёт язык для описания форм этих различений — язык паттернов связей.

> *Читая аксиомы, помните: вы работаете не с символами, ссылающимися на объекты, а с паттернами форм, задающими способы различения. Каждый символ в формуле — это уже связь, определённая своим паттерном.*

---

## Структура документации

| Раздел | Описание |
|--------|----------|
| [Онтология различения](02-ontology.md) | Минимальный онтологический акт |
| [Знаки как запросы](03-signs-as-queries.md) | Семиотика МТС |
| [Система аксиом](../02-axioms/01-overview.md) | Полное описание 12 аксиом |
| [Нотации](../03-notations/01-overview.md) | Три способа записи связей |
| [Архитектура прувера](../05-prover/01-architecture.md) | Реализация в коде |

---

## Реализация в коде

Основные концепции МТС реализованы в модулях прувера:

```typescript
// src/core/prover.ts — ядро прувера
// src/core/ast.ts — типы AST для представления связей
// src/core/parser.ts — парсинг формальной нотации
```

См. [Архитектура прувера](../05-prover/01-architecture.md) для подробностей реализации.
