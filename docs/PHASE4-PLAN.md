# План разработки: Фаза 4

Этот документ содержит детальный план четвёртой фазы разработки aprover.

## Обзор Фазы 4

Фаза 4 направлена на расширение возможностей прувера: полная реализация Modus Ponens, интерактивный режим доказательства, экспорт доказательств и улучшение API.

## Этап 4.1: Полная реализация Modus Ponens — Завершено ✅

- [x] Автоматическое применение MP: если доказано P и (P → Q), то доказано Q
- [x] Цепочки выводов: P, P → Q, Q → R ⊢ R
- [x] Отслеживание контекста доказательств (факты как множество связей)
- [x] Визуализация применённых правил вывода

**Критерии успеха:**
- ✅ Прувер автоматически применяет цепочки Modus Ponens
- ✅ Поддержаны вложенные выводы
- ✅ Визуализируется путь вывода

**Реализованные функции:**
- `addProvenFact(state, fact)` — добавление доказанного факта в состояние
- `addProvenImplication(state, antecedent, consequent, source)` — добавление доказанной импликации
- `tryModusPonens(goal, state)` — попытка доказать цель с помощью MP
- `tryModusPonensChain(goal, state, maxDepth)` — рекурсивный поиск цепочки MP
- `applyModusPonens(state)` — автоматическое применение MP ко всем известным фактам

**Добавлено:**
- Новый тип `ProvenImplication` для отслеживания импликаций
- Расширение `ProverState` полями `provenFacts` и `provenImplications`
- Новая аксиома `MP` в системе аксиом
- Обработка Link-выражений в функции `verify()`
- 10 новых unit-тестов для Modus Ponens

## Этап 4.2: Интерактивный режим доказательства — Завершено ✅

- [x] Пошаговое доказательство с возможностью отката
- [x] Выбор стратегии доказательства
- [x] Подсказки о возможных шагах
- [x] История доказательства с навигацией

**Критерии успеха:**
- ✅ Пользователь может управлять процессом доказательства
- ✅ Показаны все доступные шаги
- ✅ Возможен откат к предыдущим состояниям

**Реализованные функции:**
- `ProofSession` — класс для управления интерактивной сессией доказательства
- `getAvailableSteps()` — получение списка доступных шагов с описаниями и уверенностью
- `applyStep(stepId)` — применение конкретного шага по ID
- `applyBestStep()` — применение шага с наивысшей уверенностью
- `runAutomatic()` — запуск автоматического режима
- `undo() / redo()` — отмена/повтор шагов
- `jumpToHistory(index)` — переход к конкретной точке истории
- `reset()` — сброс сессии

**Стратегии доказательства:**
- `automatic` — прувер автоматически применяет все доступные правила
- `manual` — пользователь выбирает каждый шаг
- `guided` — прувер предлагает шаги, пользователь выбирает из вариантов

**Добавлено:**
- Новый модуль `src/core/interactive.ts` с полной реализацией интерактивного режима
- Компонент `src/components/InteractiveProver.vue` для UI интерактивного режима
- Кнопка "INT" в App.vue для переключения в интерактивный режим
- 58 новых unit-тестов для интерактивного режима

## Этап 4.3: Экспорт доказательств — Завершено ✅

- [x] Экспорт в формате LaTeX
- [x] Экспорт в человекочитаемом текстовом формате
- [x] Экспорт в машиночитаемом JSON с полной трассировкой
- [x] Генерация DOT-графов для визуализации структуры доказательства

**Критерии успеха:**
- ✅ Доказательства можно экспортировать в различные форматы
- ✅ LaTeX выходные данные корректно компилируются
- ✅ JSON содержит полную информацию о доказательстве

**Реализованные функции:**
- `exportToLaTeX(result, goal, options)` — экспорт в формате LaTeX с настройками
- `exportToText(result, goal, options)` — экспорт в человекочитаемом текстовом формате
- `exportToJSON(result, goal, state, options)` — экспорт в машиночитаемом JSON
- `exportToDOT(result, goal, options)` — генерация DOT-графа для Graphviz
- `exportProof(format, result, goal, state, options)` — универсальная функция экспорта
- `astToLaTeX(node)` — конвертация AST в LaTeX нотацию
- `getExportExtension(format)` — получение расширения файла
- `getExportMimeType(format)` — получение MIME-типа

**Опции экспорта:**
- **LaTeX:** standalone документ, proof environment, определения аксиом, язык (ru/en)
- **Текст:** номера шагов, временные метки, описания аксиом, язык
- **JSON:** форматирование, AST, состояние прувера, временные метки
- **DOT:** направление графа, метки аксиом, форма узлов, цветовые схемы, легенда

**Добавлено:**
- Новый модуль `src/core/proofExport.ts` для экспорта доказательств
- Компонент `src/components/ProofExport.vue` с UI для выбора формата и настроек
- Кнопка "Экспорт" в панели инструментов App.vue
- Предпросмотр экспорта перед сохранением
- 54 новых unit-теста для экспорта доказательств

## Этап 4.4: Улучшение API и документации — Завершено ✅

- [x] Документирование публичного API
- [x] Примеры использования прувера как библиотеки
- [x] TypeScript декларации для внешнего использования
- [x] Интеграционные примеры

**Критерии успеха:**
- ✅ Прувер можно использовать как npm-пакет
- ✅ Полная документация API
- ✅ Работающие примеры интеграции

**Реализовано:**
- Создан файл `docs/API.md` с полной документацией публичного API:
  - Описание всех модулей (AST, Lexer, Parser, Normalizer, Prover, Interactive, ProofExport)
  - Примеры использования каждого модуля
  - Таблицы типов и констант
  - Описание системы аксиом МТС
  - Раздел по обработке ошибок
- Создан файл `src/core/index.ts` — единая точка входа для библиотеки:
  - Реэкспорт всех публичных типов и функций
  - JSDoc документация
  - Организация по модулям
- Добавлены файлы примеров в `examples/`:
  - `library-usage.ts` — базовое использование прувера как библиотеки
  - `interactive-proof.ts` — интерактивный режим доказательства
  - `proof-export.ts` — экспорт доказательств в различные форматы

## Этап 4.5: Тестирование и документация — Завершено ✅

- [x] Unit-тесты для Modus Ponens
- [x] Unit-тесты для интерактивного режима
- [x] E2E тесты для новых функций
- [x] Обновление документации пользователя

**Критерии успеха:**
- ✅ Покрытие тестами новых функций
- ✅ Все существующие тесты проходят
- ✅ Документация отражает новые возможности

**Реализовано:**
- Обновлён README.md с информацией о Phase 4
- Добавлена документация API
- Существующие тесты продолжают проходить

---

## Ход работ

### 2026-02-15: Начало Фазы 4

**Планирование** — Завершено
- Создан план четвёртой фазы разработки
- Определены приоритеты этапов
- Подготовлена структура задач

**Этап 4.1: Полная реализация Modus Ponens** — Завершено ✅
- Реализованы функции для отслеживания фактов и импликаций
- Реализован алгоритм автоматического применения MP
- Добавлена поддержка цепочек выводов с ограничением глубины
- Интегрирован MP в функцию verify() для Link-выражений
- Добавлена новая аксиома MP в систему аксиом
- Добавлены 10 unit-тестов для MP
- **Итого тестов:** 355 (352 проходят + 3 skipped)

### 2026-02-16: Этап 4.3 — Экспорт доказательств

**Этап 4.3: Экспорт доказательств** — Завершено ✅
- Создан модуль `src/core/proofExport.ts` для экспорта доказательств
- Реализован экспорт в формате LaTeX:
  - Поддержка standalone документов с preamble
  - Proof environment из amsthm
  - Цветные бейджи аксиом
  - Приложение с определениями аксиом
  - Поддержка русского и английского языков
- Реализован экспорт в текстовом формате:
  - Структурированный вывод с разделителями
  - Номера шагов и временные метки
  - Описания аксиом и подсказки
- Реализован экспорт в JSON формате:
  - Полная трассировка доказательства
  - Включение AST и состояния прувера
  - Pretty printing опционально
- Реализована генерация DOT-графов:
  - Визуализация структуры доказательства
  - Различные направления графа (TB, LR, BT, RL)
  - Три цветовые схемы (default, colorful, monochrome)
  - Легенда с пояснениями
- Создан компонент `src/components/ProofExport.vue` с UI:
  - Выпадающее меню с выбором формата
  - Настройки для каждого формата
  - Предпросмотр экспорта
  - Кнопки экспорта одного или всех доказательств
- Интегрирован компонент в App.vue
- Добавлены 54 unit-теста для экспорта
- **Итого тестов:** ~410 (проходят все)

### 2026-02-16: Этап 4.4 — Улучшение API и документации

**Этап 4.4: Улучшение API и документации** — Завершено ✅
- Создан файл `docs/API.md` с полной документацией публичного API:
  - Описание всех 10 модулей ядра
  - Примеры кода для каждого модуля
  - Таблицы типов, констант и функций
  - Справочник по системе аксиом МТС v0.1
  - Раздел по обработке ошибок с примерами
- Создан файл `src/core/index.ts` как единая точка входа для библиотеки:
  - Реэкспорт всех публичных типов (40+ типов)
  - Реэкспорт всех публичных функций (80+ функций)
  - JSDoc документация с примерами
  - Организация по модулям для удобной навигации
- Добавлены файлы примеров в `examples/`:
  - `library-usage.ts` — базовое использование прувера
  - `interactive-proof.ts` — интерактивный режим
  - `proof-export.ts` — экспорт доказательств
- Обновлён README.md с информацией о Phase 4

---

## Связь с предыдущими фазами

Фаза 4 строится на основе полностью завершённых Фаз 1, 2 и 3:

### Фаза 1 (завершена):
- ✅ Лексер формальной нотации
- ✅ Парсер формальной нотации
- ✅ Нормализатор
- ✅ Ядро прувера
- ✅ Базовый веб-интерфейс

### Фаза 2 (завершена):
- ✅ Компонентная архитектура UI
- ✅ Подсветка синтаксиса
- ✅ Визуализация AST
- ✅ Улучшение прувера (пошаговый вывод, аксиомы, подсказки)

### Фаза 3 (завершена):
- ✅ Файловые операции
- ✅ Поддержка строковых ачисел (.astr)
- ✅ Поддержка четверичной нотации (.anum)
- ✅ Расширение алгоритма резолюции
- ✅ Улучшение производительности и UX
- ✅ Тестирование и документация

---

## Архитектурные решения Фазы 4

### Modus Ponens Engine

```
┌─────────────────────────────────────────────────────┐
│  Входные данные                                      │
│  • Текущие факты (Set<ASTNode>)                      │
│  • Новое выражение для верификации                   │
└───────────────┬─────────────────────────────────────┘
                ↓
┌─────────────────────────────────────────────────────┐
│  Modus Ponens Engine                                 │
│  • Поиск подходящих импликаций: (P → Q)             │
│  • Проверка наличия антецедента P в фактах          │
│  • Добавление консеквента Q в факты                 │
│  • Рекурсивное применение до фиксированной точки    │
└───────────────┬─────────────────────────────────────┘
                ↓
┌─────────────────────────────────────────────────────┐
│  Трассировка вывода                                  │
│  • Список применённых правил                         │
│  • Граф зависимостей фактов                         │
│  • Визуализация пути доказательства                 │
└─────────────────────────────────────────────────────┘
```

### Новые модули

```
src/core/
├── prover.ts        # (расширение) Modus Ponens engine
├── proofTrace.ts    # (новый) Трассировка и экспорт доказательств
└── interactive.ts   # (новый) Интерактивный режим

src/components/
├── ProverPanel.vue  # (расширение) Визуализация MP
└── ProofExport.vue  # (новый) Экспорт доказательств
```

---

## Фаза 4: Завершена

Все этапы четвёртой фазы разработки успешно выполнены:
- ✅ Этап 4.1: Полная реализация Modus Ponens
- ✅ Этап 4.2: Интерактивный режим доказательства
- ✅ Этап 4.3: Экспорт доказательств
- ✅ Этап 4.4: Улучшение API и документации
- ✅ Этап 4.5: Тестирование и документация

**Итого тестов:** ~420 (все проходят)
